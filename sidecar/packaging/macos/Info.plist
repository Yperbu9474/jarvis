<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>Jarvis</string>
	<key>CFBundleDisplayName</key>
	<string>Jarvis</string>
	<key>CFBundleIdentifier</key>
	<string>com.jarvis.sidecar</string>
	<key>CFBundleExecutable</key>
	<string>jarvis</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>__VERSION__</string>
	<key>CFBundleVersion</key>
	<string>__VERSION__</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>11.0</string>
	<!-- Menu-bar agent: no Dock icon (mirrors the tray's NSApplicationActivationPolicyAccessory). -->
	<key>LSUIElement</key>
	<true/>
	<key>NSHighResolutionCapable</key>
	<true/>
	<!-- ATS exception for web content only. Self-hosted brains are commonly
	     reached over plain HTTP on a LAN or an overlay network like Tailscale
	     (docs/SELF_HOSTING.md). ATS has always refused cleartext loads to fully
	     qualified host names, and per Apple's NSAllowsLocalNetworking docs
	     macOS 14 refuses them to raw IP addresses too, so without this key the
	     panel webview shows such a dashboard as a blank window while a browser
	     opens the same URL. The Go client's ws:// connection is not subject to
	     ATS, which is why the sidecar still connects.
	     The exemption covers every WKWebView in the process. Code pins only a
	     panel's first URL (sanitizePanelURL: the brain host, http or https
	     only, never http for a wss brain); later in-page navigations are not
	     restricted. A refused panel load is logged by panels_loadfail_darwin.go. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
	</dict>
	<!-- TCC usage strings a bundled app needs (a bare binary borrowed its parent's).
	     Screen Recording + Accessibility (global hotkeys) are prompted at runtime and
	     need no Info.plist key, but are documented in README.md. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>Jarvis listens for your voice commands and wake word.</string>
	<key>NSAppleEventsUsageDescription</key>
	<string>Jarvis controls the apps you ask it to act in.</string>
	<!-- jarvis:// deep links (the connect page's self-host enroll handoff).
	     macOS delivers URL opens to the running app as a kAEGetURL Apple Event,
	     never argv — the receiving handler lives in deeplink_darwin.go. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>Jarvis Protocol</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>jarvis</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
