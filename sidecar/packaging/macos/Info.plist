<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleName</key>
	<string>Jarvis</string>
	<key>CFBundleDisplayName</key>
	<string>Jarvis</string>
	<key>CFBundleIdentifier</key>
	<string>com.jarvis.sidecar</string>
	<key>CFBundleExecutable</key>
	<string>jarvis</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>__VERSION__</string>
	<key>CFBundleVersion</key>
	<string>__VERSION__</string>
	<key>CFBundleIconFile</key>
	<string>AppIcon</string>
	<key>LSMinimumSystemVersion</key>
	<string>11.0</string>
	<!-- Menu-bar agent: no Dock icon (mirrors the tray's NSApplicationActivationPolicyAccessory). -->
	<key>LSUIElement</key>
	<true/>
	<key>NSHighResolutionCapable</key>
	<true/>
	<!-- Self-hosted brains commonly use plain HTTP on a LAN or private overlay
	     address. ATS applies to WKWebView independently of reachability, so an
	     installed app otherwise renders those dashboards as a blank page even
	     though Safari can open the same URL. Panel navigation is still restricted
	     in code to the configured brain origin. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
	</dict>
	<!-- TCC usage strings a bundled app needs (a bare binary borrowed its parent's).
	     Screen Recording + Accessibility (global hotkeys) are prompted at runtime and
	     need no Info.plist key, but are documented in README.md. -->
	<key>NSMicrophoneUsageDescription</key>
	<string>Jarvis listens for your voice commands and wake word.</string>
	<key>NSAppleEventsUsageDescription</key>
	<string>Jarvis controls the apps you ask it to act in.</string>
	<!-- jarvis:// deep links (the connect page's self-host enroll handoff).
	     macOS delivers URL opens to the running app as a kAEGetURL Apple Event,
	     never argv — the receiving handler lives in deeplink_darwin.go. -->
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleURLName</key>
			<string>Jarvis Protocol</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>jarvis</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
